local:
  host: http://localhost:8000
  timeout: 30
  selenium:
    headless: False
  database:
    db_host: localhost
    db_port: 5432
    db_name: postgres
    db_user: postgres
    db_password: postgres
circleci:
  host: http://localhost:8000
  timeout: 30
  selenium:
    headless: True
  database:
    db_host: localhost
    db_port: 5432
    db_name: postgres
    db_user: postgres
    db_password: postgres